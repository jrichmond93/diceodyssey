import type { VoyageHomeAiProfile } from './types.js'

export const DEFAULT_TARGET_LEAGUES = 80
export const MIN_PLAYERS = 2
export const MAX_PLAYERS = 4

export const AI_PROFILE_LABEL: Record<VoyageHomeAiProfile, string> = {
  posei: 'Posei',
  odys: 'Odys',
  poly: 'Poly',
}
